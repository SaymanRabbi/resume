import React from 'react';
import Resume from './Resume';

const Professional = ({refar}) => {
    return (
        <div>
            <Resume refar={refar}/>
        </div>
    );
};

export default Professional;